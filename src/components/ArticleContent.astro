<div
  id="article-content"
  class="article-prose max-w-none animate-slide-up mt-5 border-t border-dashed border-neutral-200/80 pt-5 pb-6 dark:border-neutral-700/70 md:mt-6 md:pt-6 md:pb-10"
  style="animation-delay: 300ms;"
>
  <slot />
</div>

<style>
  @reference "../styles/global.css";

  :global(.article-prose) {
    color: #374151;
    font-family: var(--font-article);
    font-size: 1rem;
    letter-spacing: 0;
    line-height: 1.78;
    overflow-wrap: break-word;
    text-rendering: optimizeLegibility;
  }

  :global(.dark .article-prose) {
    color: var(--theme-dark-article-text);
  }

  :global(.article-prose > astro-island),
  :global(.article-prose > astro-island > div) {
    display: block;
    min-width: 0;
    max-width: 100%;
  }

  /* 兼容旧文章的 :::color 容器，其余正文排版使用 Streamdown 原生样式。 */
  :global(.article-prose .color-block) {
    --callout-color: 107 114 128;
    margin-block: 1.5em;
    border: 1px solid rgb(var(--callout-color) / 0.28);
    border-left: 3px solid rgb(var(--callout-color) / 0.8);
    border-radius: 0.4rem;
    background: rgb(var(--callout-color) / 0.06);
    padding: 0.75em 1em;
  }

  :global(.dark .article-prose .color-block) {
    background: rgb(var(--callout-color) / 0.1);
  }

  :global(.article-prose .color-block > :first-child) {
    margin-top: 0;
  }

  :global(.article-prose .color-block > :last-child) {
    margin-bottom: 0;
  }

  :global(.article-prose .color-block--red),
  :global(.article-prose .color-block--rose) {
    --callout-color: 239 68 68;
  }

  :global(.article-prose .color-block--orange),
  :global(.article-prose .color-block--amber),
  :global(.article-prose .color-block--yellow) {
    --callout-color: 245 158 11;
  }

  :global(.article-prose .color-block--lime),
  :global(.article-prose .color-block--green),
  :global(.article-prose .color-block--emerald),
  :global(.article-prose .color-block--teal) {
    --callout-color: 34 197 94;
  }

  :global(.article-prose .color-block--cyan),
  :global(.article-prose .color-block--sky),
  :global(.article-prose .color-block--blue) {
    --callout-color: 59 130 246;
  }

  :global(.article-prose .color-block--indigo),
  :global(.article-prose .color-block--violet),
  :global(.article-prose .color-block--purple),
  :global(.article-prose .color-block--fuchsia),
  :global(.article-prose .color-block--pink) {
    --callout-color: 139 92 246;
  }

  @media (min-width: 768px) {
    :global(.article-prose) {
      font-size: 0.9375rem;
    }
  }
</style>
