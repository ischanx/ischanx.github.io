# Project Instructions

## Visual Design

- 使用中性灰作为站点背景、正文、边框、卡片和代码区域的基础色，保持页面克制、易读。
- 蓝色是站点主题色，但不应铺满页面；仅用于导航选中态、链接悬停态、按钮、焦点环、标签选择、分页和其他必要的交互反馈。
- 不要把所有元素都做成纯黑白。非交互内容以中性色建立层次，交互状态使用适度的蓝色，保留产品的活力和可感知性。
- 浅色主题和深色主题都必须保持足够的文字对比度；深色主题使用较亮的蓝色交互态，避免使用难以辨识的深蓝色。
- 新增组件时优先复用全局主题 Token（`primary`、`muted`、`border`、`background` 等），不要在组件中随意写死另一套品牌色。

## Blog Cover Images

- 生成或替换博客封面图片资产时，必须使用项目 Skill：`$generate-blog-covers`（`.agents/skills/generate-blog-covers/SKILL.md`）。该 Skill 只负责调用内置 `image_gen`、筛选图片、裁切压缩并输出本地 WebP，不负责上传 CDN、修改 frontmatter、检查全站引用或执行站点构建。
- 每篇博客必须有有效的 `image` frontmatter。字段为空、文件不存在或引用失效时，为该文章独立生成封面；不得用通用占位图或同一张图片重复覆盖多篇文章。
- 封面统一采用「极简工程出版风」：浅中性灰纸张底、细黑灰线条、工程网格、流程图、结构图、手绘示意或平面编辑插画；根据文章的真实主题选择视觉隐喻，每张图片只使用一个克制的强调色。
- 构图使用正视或正交的纯平面 2D 表达。技术文章优先呈现数据流、模块关系、系统边界、算法路径或交互状态；个人记录可使用平面手绘叙事；游戏等题材使用原创线稿或像素插画，不直接复刻受版权保护的角色和场景。
- 禁止生成 3D、伪 3D、等距视角、明显透视、体积光、材质渲染、厚重阴影、拟物设备、SaaS 营销页式立体小物和无关人物。禁止可读文字、数字、代码字符、Logo、水印及随机 UI 文案。
- 使用 OG Image 标准：`1200x630`、WebP。默认使用 libwebp `quality 78`、`compression_level 6`；单张硬上限为 `200 KB`，超过时降至 `quality 72`。平面线稿压缩后低于 `80 KB` 属正常结果，不要为凑体积增加噪点。
- 主体保持完整并优先放在画面中央或稍偏右，兼顾首页卡片右侧缩略裁切；关键结构不要贴边。本地成品保存到被忽略的 `backups/cdn-images/covers/<slug>.webp`，上传后 frontmatter 使用 `https://cdn.chanx.tech/image/covers/<slug>.webp`；不得在远端文件尚未验证可访问时提前写回 URL。

## Implementation

- 修改代码时，同时考虑桌面端、移动端和不同内容长度下的响应式表现。
- 修改交互控件时，同时检查默认、悬停、聚焦、选中、禁用和深色主题状态。

## Content Metadata

- 新增、导入或迁移博客文章后，检查或修复分类与标签时必须使用项目 Skill：`$fix-blog-metadata`（`.agents/skills/fix-blog-metadata/SKILL.md`）；该 Skill 只处理分类目录和 `tags`，不得顺带改写文章内容。
- 分类是文章的一级方向，一篇文章只保留一个分类；当前分类为「软件开发」「AI」「学习」「个人记录」。分类表达文章的主要目标，不承载具体技术、文章形式或年份。
- 「学习」用于基础知识、原理、课程、阅读和非计算机学习；「软件开发」用于项目实现、系统设计、部署、工程化和性能优化；不要再用“基础/复杂”或技术栈名称作为分类边界。
- tag 是可以交叉组合的正交维度，不是分类的下级目录。它可以同时表达技术领域、知识层次和工程方向，例如 `前端` + `工程化`、`计算机` + `基础知识`；不要把两个维度合并成「计算机基础」这类不可复用的复合词。具体技术关键词直接保留在标题和正文中。
- 一篇文章通常保留 0～3 个 tag；分类已经表达清楚、标题已经明确指出、只在正文中偶然出现的内容，不再重复打 tag。
- 不要把文章形式或泛化描述当作 tag，例如「教程」「笔记」「专业课」「工具」「分享」「技术分析」「配置」「效率工具」；不要把分类名（如「前端」）重复作为 tag。
- tag 使用统一的 canonical 命名：`前端`、`后端`、`全栈`、`AI`、`小程序`、`计算机`、`基础知识`、`业务`、`系统设计`、`性能`、`安全`、`稳定性`、`工程化`、`研发体验`。`JavaScript`、`CSS`、`Vue`、`Swiper`、`Node`、`Docker` 等技术名不单独建立 tag。
- tag 词表是显式白名单，新增方向前先确认它是否形成了独立、长期的内容线；低频技术和一次性关键词直接不打 tag。
- 当前 tag 白名单为：`前端`、`后端`、`全栈`、`AI`、`小程序`、`计算机`、`基础知识`、`业务`、`系统设计`、`性能`、`安全`、`稳定性`、`工程化`、`研发体验`。其中「微信小程序」统一使用「小程序」，「前端工程化」统一使用「工程化」。
- `src/utils/navigation.ts` 是 tag 白名单的实现来源；未知 tag 可以保留在文章元数据中并以普通文本展示，但不会生成标签目录、详情页、链接或 sitemap，不得通过 fallback 自动扩张。
- 分类与 tag 的关系遵循“一个一级分类 + 多个可交叉 tag”：例如「学习」分类下的 JavaScript、CSS、Vue 基础文章使用 `前端`，Node、Java、Maven 文章使用 `后端`，算法、计算机网络、计算机组成原理等文章使用 `计算机` + `基础知识`；部署和架构类文章使用 `全栈` 或 `系统设计`，工程落地、性能优化、认证安全、开发环境等文章再按需补充 `工程化`、`性能`、`安全` 或 `研发体验`。tag 可以跨分类复用，分类不因具体技术拆分。
